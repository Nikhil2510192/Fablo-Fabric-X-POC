# Generated by armageddon tool
# See: https://github.com/hyperledger/fabric-x-orderer/tree/main/deployment
# NOTE: This is a placeholder config for POC purposes.
# In production, generate using: armageddon generate --config=config.yaml

role: router

network:
  listenAddress: "0.0.0.0:7050"

parties:
  - ID: 1
    AssemblerEndpoint: "orderer-assembler.fabric-x-local:7053"
    ConsenterEndpoint: "orderer-consenter.fabric-x-local:7052"
    RouterEndpoint: "orderer-router.fabric-x-local:7050"
    BatchersEndpoints:
      - "orderer-batcher.fabric-x-local:7051"

tls:
  enabled: false